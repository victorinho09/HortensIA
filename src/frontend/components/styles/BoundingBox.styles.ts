import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  box: {
    position: 'absolute',
    borderWidth: 2,
  },
  label: {
    position: 'absolute',
    top: -24,
    left: 0,
    paddingHorizontal: 6,
    paddingVertical: 2,
    borderRadius: 4,
  },
  labelText: {
    fontSize: 12,
    fontWeight: 'bold',
  },
});
